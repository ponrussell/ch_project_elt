name: 'course_project'
version: '1.0.0'
config-version: 2

profile: 'course_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  course_project:
    +materialized: view
    staging:
      +materialized: view